# 浏览器默认走同源 /api 与 /static（推荐用于 Docker / ModelScope）。
# 只有前后端确实部署在不同公开域名时才填写完整 URL。
NEXT_PUBLIC_API_URL=

# Next.js 服务端 rewrite 的内部 FastAPI 地址；不会暴露给浏览器。
BACKEND_INTERNAL_URL=http://localhost:8501

# 可选：如果需要直接调用后端 API
# API_BASE_URL=http://localhost:8000

