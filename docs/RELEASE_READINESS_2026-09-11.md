# 发布前收尾：2026-09-11

本轮只在本地收尾，不 push、不操作创空间。最初未提交；用户随后批准本地分组 commit，按本文末尾更新执行。适合准备开发分支/PR，不代表已达到稳定 main 合并条件。

## API 通道选择与实际证据

采用现有百炼 `qwen3.7-plus`，保持项目当前默认供应商/模型，不修改私有配置、不切换 GPU 模型档。
官方支持信息：[模型介绍](https://help.aliyun.com/zh/model-studio/qwen3-7-plus)、[兼容接口](https://help.aliyun.com/zh/model-studio/qwen-api-via-openai-chat-completions)。

`python -m scripts.release_api_smoke --run` 是需显式选择的付费 API 小规模探针：

- 只发送人工合成语句、项目规划提示词/Schema，以及虚构的测试曲目；不发送用户记忆、日志、音频或数据库内容。
- 使用固定官方 HTTPS 地址，不把密钥发往自定义代理。最多两轮 Planner（每轮允许一次原有结构修复）及一次简短介绍，共最多 5 次请求；不自动调用其他供应商。
- 本次两轮结构校验和 ToolPlan 对齐检查均通过，模式均为 recommendation；“梦幻一点的歌曲有没有”没有被当作普通闲聊。
- 两轮规划分别为 13.54 秒、13.77 秒；介绍回复非空。它们是 API 规划时间，**不是首歌或整轮耗时，也不能等同于微调 35B 的成绩**。
- 原始合成输出保存在已忽略的 `tests/eval/results/release_api_smoke_20260911.json`，不把测试输出加入训练数据。

探针复用了生产 DashScope Planner 适配器、提示词和 Schema，但没有执行整个 Agent 图、真实向量检索、记忆写入、音频播放。下一步可用此通道做全链路验收，仍需隔离数据库及测试曲库/向量运行环境。该 API 不替代 MuQ/OMAR，也不宣称全本地推理。

## 自动验收与准备工作

- CI 新增独立 music-mcp、local-gradio、neo4j-integration；前两项各装独立依赖，Neo4j 使用临时服务，无持久卷、无生产凭据，不启动 GraphZep。
- Neo4j 版本沿用项目 Compose 默认版本；集成测试仅连接测试回环端口 27687，不允许改为生产 Aura 代跑。
- 本地全仓 Ruff、Web ESLint 与生产构建通过。
- 主单元回归 **1594 passed, 3 skipped, 1 warning**（55.77 秒）；MCP **7 passed**；真实 Gradio 构建/回调 **1 passed**，保留框架资源/弃用警告说明，不声称零警告。
- CPU/CUDA/ROCm Compose 配置在候选快照内复制公开 `.env.example` 后验证通过；原工作区没有创建/覆盖 `.env`。四个启动/训练 shell 脚本语法检查通过，没有执行训练。
- Gitleaks 8.30.1 对 tracked + 未忽略新增文件的候选快照扫描：约 8.45 MB，未发现密钥。忽略的私有配置/运行数据不进入快照；此结果不代表整个历史扫描通过。
- 随后本地历史扫描实际检查 431 个 commit、约 35.41 MB，**发现 4 条告警**：`docker-compose.yml` 第 15 行的历史 NEO4J_AUTH（bc17d53e、cf995d63），以及 `config/settings.yml` 第 97 行历史 secret_key 环境变量表达式（53c6d293、92db6686）。未回显值；不能据此认定 4 个不同的有效密钥，也不能认定已撤销或均为误报。
- 历史告警是本轮新增发现的发布阻碍。需核对是否为示例/旧本地认证及是否曾用于真实环境；若是真实凭据，先撤销/轮换。不能为通过 CI 直接加入忽略列表；重写历史需单独决策与备份，当前未执行。
- 后续只读核实纠正了“secret_key 环境变量表达式”的初步描述：`${SEARXNG_SECRET}` 位于注释，实际字段为硬编码字面值，不是可以直接排除的变量占位符误报。两条历史 NEO4J_AUTH 告警的认证值均与当前进程继承的 NEO4J_PASSWORD 相符。仅比较本地值，没有连接数据库验证，不能断言运行实例仍采用此密码，也不能认定已安全轮换。
- 当前不自动修改数据库密码、系统环境或历史提交，不创建忽略规则。需要先确认旧密码使用范围，并协调数据库认证值与客户端配置一起更新；新密码不得写入仓库或聊天。轮换后再讨论历史清理/精确登记及远程 CI，不因凭据已轮换就声称历史扫描无告警。
- README 和 Changelog 增加入口、边界与迁移说明；可选原型没有宣称正式产品交付。

## 合并 main 前仍需确认

1. 专用 Neo4j 实际运行回执、约束、重复写入和删除顺序测试；本机 Docker 引擎未运行，本轮未实测。新 CI 任务尚未推送执行，不能计为通过。
2. API 模型 + 隔离 catalog 的主 API 多轮/取消/记忆/播放验收；真实浏览器与声学检索尚未完成。
3. 处理上述历史敏感值告警，然后经用户授权推送开发分支，检查远程 CI/CodeQL。未核实前不宣称安全门禁通过，不修改或重写历史掩盖结果。

## 原建议提交拆分

1. 请求隔离、流生命周期、工具预算、身份与错误边界及配套测试。
2. 检索曲目 ID、GPU 档位、入库恢复及配套测试。
3. 记忆有效期、授权组件、持久待办/回执、恢复 UI 及配套测试。
4. CLI/MCP/Gradio 实验入口、独立依赖和 CI。
5. 发布说明与审计证据。每个提交包含其必需的新增文件，避免只提交已跟踪修改而漏掉实现。

停用 GraphZep 的历史安全修复单独成组审查，不因整理提交重新启动或启用它。
上述拆分需要逐组检查依赖与测试；不要直接 `git add .`，不要自动部署。

## 本地提交授权与实际分组

用户随后明确批准“先本地分组提交”。依赖核对后，核心 API、请求隔离、检索与记忆恢复存在交叉引用，不拆成缺少实现的中间提交。采用以下五组，仅保存本地历史：

1. 可选 GraphZep 的已有安全边界修复与忽略规则；不启动、不启用该服务。
2. 核心推荐/检索、请求和身份隔离、记忆授权与恢复、配套 UI 和测试。
3. 入库租约/阶段恢复、音频获取与保留边界。
4. 本地 CLI、音乐 MCP、Gradio 适配器及独立依赖/测试。
5. CI、受控 API 探针、发布和审计文档。

不提交私有配置、密钥、模型、音频、虚拟环境和运行结果；不处理历史告警的忽略/重写，不推送远程。历史安全告警、真实数据库与全链路验收状态不因本地 commit 而改变。

## 非本轮合并的前置条件

创空间资源恢复与旧 UI 完整迁移、MCP 写工具/完整插件、ToolPlan 默认执行切换、历史数据物理清除、多副本/备份恢复、小模型和 MuQ+OMAR 消融可继续列为后续里程碑，不以它们无限拖延本轮可靠性更新。
