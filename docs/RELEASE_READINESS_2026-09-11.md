# 发布验证：2026-09-11

## 本轮内容

- 推荐请求隔离、上下文传递、歌曲优先的流式输出与错误处理。
- 结构化记忆恢复、入库租约与音频获取边界。
- 复用主 API 的 CLI、音乐 MCP 和 Gradio 适配器。
- API 原生联网检索、来源校验及当前部署配置。
- 独立测试任务、文档与使用说明。

## 验证结果

- 本地主单元测试：1596 passed，3 skipped；跳过项依赖本机缺少的数据集库或符号链接能力。
- Ruff、前端 lint/build 通过。
- CPU/CUDA/ROCm Compose 配置检查通过。
- 独立 MCP 测试 7 项通过；Gradio 组件与回调烟测通过。
- [PR #76](https://github.com/hgsanyang/SoulTuner-Agent/pull/76) 初始版本的 11 项远程检查通过，包括隔离 Neo4j 集成测试、主单元测试、前端构建、适配器测试和 CodeQL。后续提交以各自的 CI 结果为准。

## API 探针与适用范围

使用项目配置的百炼 qwen3.7-plus，对两条合成需求调用生产 Planner 适配器。
结构和对齐校验均通过，均识别为推荐需求；规划耗时分别为 13.54 秒与 13.77 秒。
合成曲目的介绍回复正常。以上不是整轮检索延迟，也不是微调 35B 的性能结果。

CLI、MCP 与 Gradio 复用主 API；适配器测试采用合成事件。
真实模型与完整曲库的多轮、声学检索和浏览器播放验收仍需独立执行。
本轮不包含创空间重新部署、模型训练或完整展示端迁移。

## 后续里程碑

- 在隔离测试环境完成 API、目录、记忆和播放的联合验收。
- 完善展示端迁移与多用户持久化。
- 按独立研究计划评估小模型与 MuQ/OMAR 组合。
